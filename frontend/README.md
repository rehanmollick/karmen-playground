# Karmen Playground — frontend

Next.js 14 (App Router) + TypeScript + Tailwind. Setup, env vars, and the
full architecture overview live in the [root README](../README.md).

```bash
npm install
npm run dev   # expects the backend on http://localhost:8000
```
